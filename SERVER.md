# Servidor

```sh
# Ter o node instalado
# Instalar todos os pacotes
npm i
# Configure o .env.local
npm run migrate
# Seed é opcional
npm run seed

# build do next
npm run build
npm start # apenas para teste
```
